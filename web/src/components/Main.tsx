import type { User } from "../typings/user";
import { locale } from "../utils/locale";

const Main: React.FC<{
    user: User;
}> = ({ user }) => {
    return (
        <main>
            
        </main>
    )
};

export default Main;